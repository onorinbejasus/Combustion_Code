#ifndef ENUMS_HH
#define ENUMS_HH

enum InteractionMode{
	ExploreMode = 0,
	SelectMode
};

enum SelectedObjectCatagory{
	NO_OBJECT = 0,
	STREAMLINE,
	SEEDPOINT
};

#endif /* end of include guard: ENUMS_HH */